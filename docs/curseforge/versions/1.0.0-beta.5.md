<h2>v1.0.0-beta.5 - Translation Investigation Notes</h2>

<h3>Documented</h3>
<ul>
<li><strong>Better Villager Animations</strong>: investigated the mod's 14 <code>lang/en_us.json</code> keys
  (profession names, <code>offer.rare_for</code>) &mdash; already 100% covered by this pack's
  <code>es_es.json</code>, nothing missing. The English text still visible in villagers' dialogue bubbles
  (greetings, ambient chatter, trade/threat lines) doesn't come from those keys: it's hardcoded as Java
  string literals directly in the mod's bytecode (<code>VillagerDialogueCatalog</code>,
  <code>VillagerConversationCatalog</code>, <code>VillagerDialogueExpansion</code>,
  <code>VillagerThreatDialogueCatalog</code>, ~136 lines total), never routed through a lang key. Not
  translatable from a resource pack.</li>
<li>No content changes in <code>resourcepack/</code> &mdash; this release only records the investigation
  in the version history.</li>
</ul>

<hr>

<p><strong>ZIP</strong>: <code>EnchantVenture_Pack-1.0.0-beta.5.zip</code></p>
